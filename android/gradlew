#!/bin/bash
# Wrapper script for Gradle 8.7

cd "$(dirname "$0")/.."

BASE_DIR="$(pwd)"

# Use the embedded gradle wrapper
cd "$BASE_DIR"

export TERM=dumb

JAVA_EXE="java"
if [ -n "$JAVA_HOME" ]; then
  JAVA_EXE="$JAVA_HOME/bin/java"
fi

"$JAVA_EXE" -jar gradle/wrapper/gradle-wrapper.jar "$@"